{
  "name": "local-growth-os",
  "version": "0.0.0",
  "private": true,
  "description": "Reusable growth, analytics, SEO, and automation OS for local service businesses.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate-config": "tsx src/config/cli.ts",
    "build-warehouse": "tsx src/warehouse/duckdb/cli.ts",
    "import-lead-log": "tsx src/connectors/sheet-lead-log/cli.ts"
  },
  "dependencies": {
    "@duckdb/node-api": "^1.5.4-r.1",
    "yaml": "^2.5.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.2"
  }
}
